<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/style.css') }}"/>
        {% endblock %}
    </head>
    <body>
        <div class="container">
            {% block body %}
                <header>
                        <div class="logo"><a href="{{ path('home') }}">DV</a></div>
                            <nav>
                                <ul>
                                    <li><a class="{{ app.request.get('_route') == 'home' ? 'active' }}" href="{{ path('home') }}">Home</a></li>
                                    <li><a class="{{ app.request.get('_route') == 'article_create' ? 'active' }}"  href="{{ path('article_create') }}">Create Article</a></li>
                                    <li><a class="{{ app.request.get('_route') == 'category_create' ? 'active' }}"  href="{{ path('category_create') }}">Create Category</a></li>
                                    <li><a class="{{ app.request.get('_route') == 'user_registration' ? 'active' }}"  href="{{ path('user_registration') }}">Registration</a></li>
                                </ul>
                            </nav>
                        {% for message in app.flashes('success') %}
                            <div class="alert alert-success">
                                {{ message }}
                            </div>
                        {% endfor %}
                </header>
                    {% block content %}

                    {% endblock %}
                <footer>
                        <h2> &copy; Default value</h2>
                </footer>
            {% endblock %}
        </div>
    </body>
</html>
